services:
  - type: web
    name: kaspi-scraper
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python kaspi_scraper_with_cookies.py
    envVars:
      - key: DATABASE_URL
        sync: false